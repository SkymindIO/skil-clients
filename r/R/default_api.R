# Endpoints
#
# Endpoints API for different services in SKIL
#
# The version of the OpenAPI document: 1.2.2
# 
# Generated by: https://openapi-generator.tech

#' @title Default operations
#' @description skil_client.Default
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' AccumulatedResults Tells how many retraining examples have labels associated with them.
#'
#'
#' AddCredentials Adds credentials
#'
#'
#' AddEvaluationResult Adds an evaluation result
#'
#'
#' AddExampleForBatch Adds a number of examples to a minibatch ID given an AddExampleRequest.
#'
#'
#' AddExampleToMinibatch Adds an example to a minibatch
#'
#'
#' AddExperiment Add an experiment, given an experiment entity
#'
#'
#' AddFeedbackBinary 
#'
#'
#' AddFeedbackJson Gets the retraining feedback for the given batch ID.
#'
#'
#' AddMinibatch Adds a minibatch
#'
#'
#' AddModelFeedback Adds an evaluation feedback to the model against a given minibatch id.
#'
#'
#' AddModelHistory Add a model history / workspace
#'
#'
#' AddModelInstance Adds a model
#'
#'
#' AddResource Adds a resource
#'
#'
#' AddResourceGroup Adds a resource group
#'
#'
#' AddResourceToGroup Adds a resource to a resource group
#'
#'
#' AggregateModelResults Aggregates the evaluaition results of a model instance, based on the evaluation type
#'
#'
#' ChangeUserPassword Change user&#39;s password
#'
#'
#' Classify Use the deployed model to classify the input
#'
#'
#' Classifyarray Same as /classify but returns the output as Base64NDArrayBody
#'
#'
#' Classifyimage Use the deployed model to classify the input, using input image file from multipart form data.
#'
#'
#' ClearState Clears the accumulated data for retraining.
#'
#'
#' CreateJob Create a job
#'
#'
#' CreateModelHistory Creates model History
#'
#'
#' DeleteCredentialsById Delete credentials given an ID
#'
#'
#' DeleteExperiment Deletes an experiment, given an experiment entity
#'
#'
#' DeleteJobById Deletes a job given its ID
#'
#'
#' DeleteModel Delete a model by deployment and model id
#'
#'
#' DeleteModelHistory Deletes a model history / workspace, given its ID
#'
#'
#' DeleteModelInstance Deletes a model instance, given its ID
#'
#'
#' DeleteResourceById Delete the resource with the specified resource ID
#'
#'
#' DeleteResourceFromGroup Removes a resource from a resource group
#'
#'
#' DeleteResourceGroupById Delete the resource group with the specified resource group ID
#'
#'
#' DeployModel Deploy a model in a deployment group.
#'
#'
#' DeploymentCreate Create a new deployment group.
#'
#'
#' DeploymentDelete Delete a deployment by id
#'
#'
#' DeploymentGet Get a deployment details by id
#'
#'
#' Deployments Get a list of deployments
#'
#'
#' Detectobjects Detect the objects, given a (input) prediction request
#'
#'
#' DownloadJobOutputFile Download the output file from the job&#39;s execution. This will ONLY work if the job&#39;s run status is &#39;COMPLETE&#39;.
#'
#'
#' GenerateAuthToken Generate new auth token
#'
#'
#' GetAllJobs Get a list of all available jobs
#'
#'
#' GetArray Get the memory mapped array based on the array type.
#'
#'
#' GetArrayIndices Get the memory mapped array indices based on the array type.
#'
#'
#' GetArrayRange Get the memory mapped array within a range based on the array type.
#'
#'
#' GetAuthPolicy Get auth policy
#'
#'
#' GetBestModelAmongModelIds Gets the best model among the given model instance IDs, based on the evaluation type and column metric
#'
#'
#' GetCredentialsById Get credentials given an ID
#'
#'
#' GetCurrentModel Returns the current model being used for retraining.
#'
#'
#' GetEvaluationForModelID Gets the list of evaluation results entity, given a model instance ID
#'
#'
#' GetExamplesForMinibatch Gets all the examples for a minibatch ID
#'
#'
#' GetExperiment Obtain an experiment&#39;s details, given its ID
#'
#'
#' GetExperimentsForModelHistory Obtain all experiments for a model history / workspace
#'
#'
#' GetJobById Get a job by its ID
#'
#'
#' GetLastEvaluation Get the last evaluation specifications from the current model.
#'
#'
#' GetMinibatch Gets a minibatch for the model
#'
#'
#' GetModelDetails Get model details
#'
#'
#' GetModelHistory Gets a model history, given its ID
#'
#'
#' GetModelInstance Gets a model instance, given its ID
#'
#'
#' GetModelsForExperiment Obtain a list of all the models for an experiment
#'
#'
#' GetResourceById Get the resource with the specified resource ID
#'
#'
#' GetResourceBySubType Get all the resources with the specified resource subtype
#'
#'
#' GetResourceByType Get all the resources with the specified resource type
#'
#'
#' GetResourceDetailsById Get the resource details with the specified resource ID
#'
#'
#' GetResourceGroupById Get the resource group with the specified resource group ID
#'
#'
#' GetResourceGroups Get a list of all the resource groups
#'
#'
#' GetResources A list of all known/registered resources, of all types
#'
#'
#' GetResourcesFromGroup Get all resources from a resource group
#'
#'
#' GetRoles Get all roles.
#'
#'
#' GetUser Get a user by user ID
#'
#'
#' GetUserAuthTokens Get auth tokens for a user.
#'
#'
#' GetUsers Get all users.
#'
#'
#' IsTraining Get the retraining status
#'
#'
#' Jsonarray Run inference on the input and returns it as a JsonArrayResponse
#'
#'
#' Knn Runs knn on the given index with the given k
#'
#'
#' Knnnew Run a k nearest neighbors search on a NEW data point
#'
#'
#' ListAllExperiments List all of the experiments in every model history / workspace
#'
#'
#' Logfilepath Get logs file path
#'
#'
#' Login Post JSON credentials and obtain a JWT authorization token.
#'
#'
#' Logs Get logs
#'
#'
#' MetaGet this method can be used to get the meta data for the current model which set to the server
#'
#'
#' MetaPost This method can be used to set meta data for the current model which is set to the server
#'
#'
#' ModelStateChange Modify the state (start/stop) of a deployed model
#'
#'
#' Models Retrieve a list of all the deployed models given a deployment id
#'
#'
#' Modelset Set the model to be served
#'
#'
#' Modelupdate Update the model to be served
#'
#'
#' Multiclassify Represents all of the labels for a given classification
#'
#'
#' Multipredict Get the output from the network, based on the given INDArray[] input
#'
#'
#' Multipredictimage Get the output from the network using the given image file using the /multipredict endpoint&#39;s method
#'
#'
#' NumRevisions Gets the number of retrained models written with retraining.
#'
#'
#' Predict Run inference on the input array.
#'
#'
#' PredictError Runs inference and find invalid rows based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' PredictV2File Runs inference based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' PredictV2Json Runs inference based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' Predictimage Run inference on the input array, using input image file from multipart form data.
#'
#'
#' Predictwithpreprocess Preprocesses the input and run inference on it
#'
#'
#' Predictwithpreprocessjson Preprocesses the input and run inference on it and returns it as a JsonArrayResponse
#'
#'
#' RawPredictBinary Runs inference based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' RefreshJobStatus Refresh the remote job status. Can be used for monitoring.
#'
#'
#' RegisterUser Register a new user
#'
#'
#' ReimportModel Reimport a model to a previous deployed model in a deployment
#'
#'
#' RevokeUserToken Revoke a user token.
#'
#'
#' Rollback Rollback to a previous revision of the model.
#'
#'
#' RunAJob Start running an (already created) job on the remote resource
#'
#'
#' TransformCsv Takes a BatchCSVRecord and returns the transformed array as BatchCSVRecord
#'
#'
#' Transformarray Takes a batch input arrays and transforms it
#'
#'
#' Transformimage Takes multiple multipart image file to transform and returns Base64NDArrayBody
#'
#'
#' TransformincrementalCsv Takes SingleCSVRecord as input and returns the transformed array as SingleCSVRecord
#'
#'
#' Transformincrementalarray Same as /transformincremental but returns Base64NDArrayBody.
#'
#'
#' Transformincrementalimage Takes a single multipart image file to transform and returns Base64NDArrayBody
#'
#'
#' TransformprocessGet Gets the JSON string of the deployed transform process (CSV or Image)
#'
#'
#' TransformprocessPost Sets the deployed (CSV or Image) transform process through the provided JSON string
#'
#'
#' UpdateAuthPolicy Update auth policy
#'
#'
#' UpdateBestModelForExperiment Updates the best model for an experiment
#'
#'
#' UpdateExperiment Updates an experiment, given an experiment entity
#'
#'
#' UpdateModelHistory Update a model history / workspace
#'
#'
#' UpdateUser Update a user
#'
#'
#' Upload Upload a model file to SKIL for import.
#'
#' }
#'
#' @importFrom caTools base64encode
#' @export
DefaultApi <- R6::R6Class(
  'DefaultApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    AccumulatedResults = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/accumulatedresults"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AccumulatedResults$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddCredentials = function(add.credentials.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`add.credentials.request`)) {
        stop("Missing required parameter `add.credentials.request`.")
      }

      if (!missing(`add.credentials.request`)) {
        body <- `add.credentials.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/resources/credentials"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ResourceCredentials$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddEvaluationResult = function(model.history.server.id, evaluation.results.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`evaluation.results.entity`)) {
        stop("Missing required parameter `evaluation.results.entity`.")
      }

      if (!missing(`evaluation.results.entity`)) {
        body <- `evaluation.results.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/revisions/evaluations/"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EvaluationResultsEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddExampleForBatch = function(model.history.server.id, add.example.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`add.example.request`)) {
        stop("Missing required parameter `add.example.request`.")
      }

      if (!missing(`add.example.request`)) {
        body <- `add.example.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/exampleForBatch"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AddExampleRequest$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddExampleToMinibatch = function(model.history.server.id, example.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`example.entity`)) {
        stop("Missing required parameter `example.entity`.")
      }

      if (!missing(`example.entity`)) {
        body <- `example.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/example"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExampleEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddExperiment = function(model.history.server.id, experiment.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`experiment.entity`)) {
        stop("Missing required parameter `experiment.entity`.")
      }

      if (!missing(`experiment.entity`)) {
        body <- `experiment.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExperimentEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddFeedbackBinary = function(id, type, file=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }

      body <- list(
        "file" = httr::upload_file(file)
      )

      urlPath <- "/feedback/{id}/{type}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), URLencode(as.character(`id`), reserved = TRUE), urlPath)
      }

      if (!missing(`type`)) {
        urlPath <- gsub(paste0("\\{", "type", "\\}"), URLencode(as.character(`type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        FeedbackResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddFeedbackJson = function(id, labels=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (!missing(`labels`)) {
        body <- `labels`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/feedback/{id}/json"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), URLencode(as.character(`id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        FeedbackResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddMinibatch = function(model.history.server.id, minibatch.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`minibatch.entity`)) {
        stop("Missing required parameter `minibatch.entity`.")
      }

      if (!missing(`minibatch.entity`)) {
        body <- `minibatch.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/minibatch"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        MinibatchEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddModelFeedback = function(model.history.server.id, model.feed.back.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.feed.back.request`)) {
        stop("Missing required parameter `model.feed.back.request`.")
      }

      if (!missing(`model.feed.back.request`)) {
        body <- `model.feed.back.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/feedback"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelFeedBackRequest$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddModelHistory = function(model.history.server.id, add.model.history.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`add.model.history.request`)) {
        stop("Missing required parameter `add.model.history.request`.")
      }

      if (!missing(`add.model.history.request`)) {
        body <- `add.model.history.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/modelhistory"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelHistoryEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddModelInstance = function(model.history.server.id, model.instance.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.instance.entity`)) {
        stop("Missing required parameter `model.instance.entity`.")
      }

      if (!missing(`model.instance.entity`)) {
        body <- `model.instance.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelInstanceEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddResource = function(add.resource.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`add.resource.request`)) {
        stop("Missing required parameter `add.resource.request`.")
      }

      if (!missing(`add.resource.request`)) {
        body <- `add.resource.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/resources/add/resource"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        object$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddResourceGroup = function(group.name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`group.name`)) {
        stop("Missing required parameter `group.name`.")
      }

      if (!missing(`group.name`)) {
        body <- `group.name`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/resources/add/group"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ResourceGroup$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AddResourceToGroup = function(resource.group.id, resource.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.group.id`)) {
        stop("Missing required parameter `resource.group.id`.")
      }

      if (missing(`resource.id`)) {
        stop("Missing required parameter `resource.id`.")
      }

      urlPath <- "/resources/add/resourcetogroup/{resourceGroupId}/{resourceId}"
      if (!missing(`resource.group.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), URLencode(as.character(`resource.group.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`resource.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), URLencode(as.character(`resource.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    AggregateModelResults = function(model.history.server.id, aggregate.prediction, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`aggregate.prediction`)) {
        stop("Missing required parameter `aggregate.prediction`.")
      }

      if (!missing(`aggregate.prediction`)) {
        body <- `aggregate.prediction`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/aggregateresults"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EvaluationResultsEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ChangeUserPassword = function(user.id, change.password.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      if (missing(`change.password.request`)) {
        stop("Missing required parameter `change.password.request`.")
      }

      if (!missing(`change.password.request`)) {
        body <- `change.password.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/user/{userId}/password"
      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), URLencode(as.character(`user.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        User$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Classify = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/classify"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ClassificationResult$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Classifyarray = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/classifyarray"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Base64NDArrayBody$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Classifyimage = function(deployment.name, version.name, model.name, image=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      body <- list(
        "image" = httr::upload_file(image)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/classifyimage"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ClassificationResult$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ClearState = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/clear"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        FeedbackResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateJob = function(job.id.or.type, create.job.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`job.id.or.type`)) {
        stop("Missing required parameter `job.id.or.type`.")
      }

      if (missing(`create.job.request`)) {
        stop("Missing required parameter `create.job.request`.")
      }

      if (!missing(`create.job.request`)) {
        body <- `create.job.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/jobs/{jobIdOrType}"
      if (!missing(`job.id.or.type`)) {
        urlPath <- gsub(paste0("\\{", "jobIdOrType", "\\}"), URLencode(as.character(`job.id.or.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JobEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    CreateModelHistory = function(model.history.server.id, model.history.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.history.entity`)) {
        stop("Missing required parameter `model.history.entity`.")
      }

      if (!missing(`model.history.entity`)) {
        body <- `model.history.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/revisions"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelHistoryEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteCredentialsById = function(credential.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credential.id`)) {
        stop("Missing required parameter `credential.id`.")
      }

      urlPath <- "/resources/credentials/{credentialId}"
      if (!missing(`credential.id`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credential.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteExperiment = function(model.history.server.id, experiment.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`experiment.id`)) {
        stop("Missing required parameter `experiment.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`experiment.id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), URLencode(as.character(`experiment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        InlineResponse200$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteJobById = function(job.id.or.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`job.id.or.type`)) {
        stop("Missing required parameter `job.id.or.type`.")
      }

      urlPath <- "/jobs/{jobIdOrType}"
      if (!missing(`job.id.or.type`)) {
        urlPath <- gsub(paste0("\\{", "jobIdOrType", "\\}"), URLencode(as.character(`job.id.or.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteModel = function(deployment.id, model.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      if (missing(`model.id`)) {
        stop("Missing required parameter `model.id`.")
      }

      urlPath <- "/deployment/{deploymentId}/model/{modelId}"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`model.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        InlineResponse200$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteModelHistory = function(model.history.server.id, model.history.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.history.id`)) {
        stop("Missing required parameter `model.history.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/modelhistory/{modelHistoryID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.history.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), URLencode(as.character(`model.history.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        InlineResponse200$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteModelInstance = function(model.history.server.id, model.instance.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.instance.id`)) {
        stop("Missing required parameter `model.instance.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/{modelInstanceID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.instance.id`)) {
        urlPath <- gsub(paste0("\\{", "modelInstanceID", "\\}"), URLencode(as.character(`model.instance.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteResourceById = function(resource.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.id`)) {
        stop("Missing required parameter `resource.id`.")
      }

      urlPath <- "/resources/remove/resource/{resourceId}"
      if (!missing(`resource.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), URLencode(as.character(`resource.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Resource$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteResourceFromGroup = function(resource.group.id, resource.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.group.id`)) {
        stop("Missing required parameter `resource.group.id`.")
      }

      if (missing(`resource.id`)) {
        stop("Missing required parameter `resource.id`.")
      }

      urlPath <- "/resources/remove/resourcefromgroup/{resourceGroupId}/{resourceId}"
      if (!missing(`resource.group.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), URLencode(as.character(`resource.group.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`resource.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), URLencode(as.character(`resource.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeleteResourceGroupById = function(resource.group.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.group.id`)) {
        stop("Missing required parameter `resource.group.id`.")
      }

      urlPath <- "/resources/remove/group/{resourceGroupId}"
      if (!missing(`resource.group.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), URLencode(as.character(`resource.group.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ResourceGroup$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeployModel = function(deployment.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment/{deploymentId}/model"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeploymentCreate = function(body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        DeploymentResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeploymentDelete = function(deployment.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      urlPath <- "/deployment/{deploymentId}"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        InlineResponse200$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DeploymentGet = function(deployment.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      urlPath <- "/deployment/{deploymentId}"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        DeploymentResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Deployments = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/deployments"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        DeploymentResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Detectobjects = function(deployment.name, version.name, model.name, id, needs.preprocessing, threshold, file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`needs.preprocessing`)) {
        stop("Missing required parameter `needs.preprocessing`.")
      }

      if (missing(`threshold`)) {
        stop("Missing required parameter `threshold`.")
      }

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      body <- list(
        "id" = id,
        "needsPreprocessing" = needs.preprocessing,
        "threshold" = threshold,
        "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/detectobjects"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        DetectionResult$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    DownloadJobOutputFile = function(job.id, download.output.file.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`job.id`)) {
        stop("Missing required parameter `job.id`.")
      }

      if (missing(`download.output.file.request`)) {
        stop("Missing required parameter `download.output.file.request`.")
      }

      if (!missing(`download.output.file.request`)) {
        body <- `download.output.file.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/jobs/{jobId}/outputfile"
      if (!missing(`job.id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), URLencode(as.character(`job.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GenerateAuthToken = function(token.generate.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`token.generate.request`)) {
        stop("Missing required parameter `token.generate.request`.")
      }

      if (!missing(`token.generate.request`)) {
        body <- `token.generate.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/auth/token"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Token$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAllJobs = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/jobs"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JobEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetArray = function(accept, array.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`accept`)) {
        stop("Missing required parameter `accept`.")
      }

      if (missing(`array.type`)) {
        stop("Missing required parameter `array.type`.")
      }

      headerParams['accept'] <- `accept`

      urlPath <- "/array/{arrayType}"
      if (!missing(`array.type`)) {
        urlPath <- gsub(paste0("\\{", "arrayType", "\\}"), URLencode(as.character(`array.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetArrayIndices = function(content.type, accept, array.type, input=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`content.type`)) {
        stop("Missing required parameter `content.type`.")
      }

      if (missing(`accept`)) {
        stop("Missing required parameter `accept`.")
      }

      if (missing(`array.type`)) {
        stop("Missing required parameter `array.type`.")
      }

      headerParams['Content-Type'] <- `content.type`

      headerParams['accept'] <- `accept`

      if (!missing(`input`)) {
        body <- `input`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/array/indices/{arrayType}"
      if (!missing(`array.type`)) {
        urlPath <- gsub(paste0("\\{", "arrayType", "\\}"), URLencode(as.character(`array.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetArrayRange = function(accept, array.type, from, to, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`accept`)) {
        stop("Missing required parameter `accept`.")
      }

      if (missing(`array.type`)) {
        stop("Missing required parameter `array.type`.")
      }

      if (missing(`from`)) {
        stop("Missing required parameter `from`.")
      }

      if (missing(`to`)) {
        stop("Missing required parameter `to`.")
      }

      headerParams['accept'] <- `accept`

      urlPath <- "/array/range/{from}/{to}/{arrayType}"
      if (!missing(`array.type`)) {
        urlPath <- gsub(paste0("\\{", "arrayType", "\\}"), URLencode(as.character(`array.type`), reserved = TRUE), urlPath)
      }

      if (!missing(`from`)) {
        urlPath <- gsub(paste0("\\{", "from", "\\}"), URLencode(as.character(`from`), reserved = TRUE), urlPath)
      }

      if (!missing(`to`)) {
        urlPath <- gsub(paste0("\\{", "to", "\\}"), URLencode(as.character(`to`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetAuthPolicy = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/auth/policy"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AuthPolicy$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetBestModelAmongModelIds = function(model.history.server.id, best.model, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`best.model`)) {
        stop("Missing required parameter `best.model`.")
      }

      if (!missing(`best.model`)) {
        body <- `best.model`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/best"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelInstanceEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetCredentialsById = function(credential.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`credential.id`)) {
        stop("Missing required parameter `credential.id`.")
      }

      urlPath <- "/resources/credentials/{credentialId}"
      if (!missing(`credential.id`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), URLencode(as.character(`credential.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ResourceCredentials$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetCurrentModel = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/model"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetEvaluationForModelID = function(model.history.server.id, model.instance.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.instance.id`)) {
        stop("Missing required parameter `model.instance.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/revisions/evaluations/{modelInstanceID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.instance.id`)) {
        urlPath <- gsub(paste0("\\{", "modelInstanceID", "\\}"), URLencode(as.character(`model.instance.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EvaluationResultsEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetExamplesForMinibatch = function(model.history.server.id, minibatch.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`minibatch.id`)) {
        stop("Missing required parameter `minibatch.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/example/{minibatchId}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`minibatch.id`)) {
        urlPath <- gsub(paste0("\\{", "minibatchId", "\\}"), URLencode(as.character(`minibatch.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExampleEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetExperiment = function(model.history.server.id, experiment.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`experiment.id`)) {
        stop("Missing required parameter `experiment.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`experiment.id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), URLencode(as.character(`experiment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExperimentEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetExperimentsForModelHistory = function(model.history.server.id, model.history.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.history.id`)) {
        stop("Missing required parameter `model.history.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiments/{modelHistoryID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.history.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), URLencode(as.character(`model.history.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExperimentEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetJobById = function(job.id.or.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`job.id.or.type`)) {
        stop("Missing required parameter `job.id.or.type`.")
      }

      urlPath <- "/jobs/{jobIdOrType}"
      if (!missing(`job.id.or.type`)) {
        urlPath <- gsub(paste0("\\{", "jobIdOrType", "\\}"), URLencode(as.character(`job.id.or.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JobEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetLastEvaluation = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/lastevaluation"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        EvaluationResultsEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetMinibatch = function(model.history.server.id, minibatch.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`minibatch.id`)) {
        stop("Missing required parameter `minibatch.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/minibatch/{minibatchId}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`minibatch.id`)) {
        urlPath <- gsub(paste0("\\{", "minibatchId", "\\}"), URLencode(as.character(`minibatch.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        MinibatchEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetModelDetails = function(deployment.id, model.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      if (missing(`model.id`)) {
        stop("Missing required parameter `model.id`.")
      }

      urlPath <- "/deployment/{deploymentId}/model/{modelId}"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`model.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetModelHistory = function(model.history.server.id, model.history.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.history.id`)) {
        stop("Missing required parameter `model.history.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/revision/{modelHistoryID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.history.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), URLencode(as.character(`model.history.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelHistoryEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetModelInstance = function(model.history.server.id, model.instance.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.instance.id`)) {
        stop("Missing required parameter `model.instance.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/{modelInstanceID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.instance.id`)) {
        urlPath <- gsub(paste0("\\{", "modelInstanceID", "\\}"), URLencode(as.character(`model.instance.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelInstanceEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetModelsForExperiment = function(model.history.server.id, experiment.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`experiment.id`)) {
        stop("Missing required parameter `experiment.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}/models"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`experiment.id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), URLencode(as.character(`experiment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelInstanceEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourceById = function(resource.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.id`)) {
        stop("Missing required parameter `resource.id`.")
      }

      urlPath <- "/resources/resource/{resourceId}"
      if (!missing(`resource.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), URLencode(as.character(`resource.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Resource$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourceBySubType = function(resource.sub.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.sub.type`)) {
        stop("Missing required parameter `resource.sub.type`.")
      }

      urlPath <- "/resources/resources/subtype/{resourceSubType}"
      if (!missing(`resource.sub.type`)) {
        urlPath <- gsub(paste0("\\{", "resourceSubType", "\\}"), URLencode(as.character(`resource.sub.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Resource$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourceByType = function(resource.type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.type`)) {
        stop("Missing required parameter `resource.type`.")
      }

      urlPath <- "/resources/resources/type/{resourceType}"
      if (!missing(`resource.type`)) {
        urlPath <- gsub(paste0("\\{", "resourceType", "\\}"), URLencode(as.character(`resource.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Resource$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourceDetailsById = function(resource.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.id`)) {
        stop("Missing required parameter `resource.id`.")
      }

      urlPath <- "/resources/details/{resourceId}"
      if (!missing(`resource.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), URLencode(as.character(`resource.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        object$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourceGroupById = function(resource.group.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.group.id`)) {
        stop("Missing required parameter `resource.group.id`.")
      }

      urlPath <- "/resources/group/{resourceGroupId}"
      if (!missing(`resource.group.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), URLencode(as.character(`resource.group.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ResourceGroup$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourceGroups = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/resources/groups"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ResourceGroup$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResources = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/resources/resources"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Resource$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetResourcesFromGroup = function(resource.group.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`resource.group.id`)) {
        stop("Missing required parameter `resource.group.id`.")
      }

      urlPath <- "/resources/group/{resourceGroupId}/resources"
      if (!missing(`resource.group.id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), URLencode(as.character(`resource.group.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Resource$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetRoles = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/security/roles"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Role$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetUser = function(user.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      urlPath <- "/user/{userId}"
      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), URLencode(as.character(`user.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        User$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetUserAuthTokens = function(user.id, only.valid=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`user.id`)) {
        stop("Missing required parameter `user.id`.")
      }

      queryParams['onlyValid'] <- only.valid

      urlPath <- "/auth/{userId}/tokens"
      if (!missing(`user.id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), URLencode(as.character(`user.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Token$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    GetUsers = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/security/users"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        User$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    IsTraining = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/istraining"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        RetrainingStatus$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Jsonarray = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/jsonarray"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JsonArrayResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Knn = function(deployment.name, version.name, knn.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`knn.name`)) {
        stop("Missing required parameter `knn.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/knn/{knnName}/{versionName}/knn"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`knn.name`)) {
        urlPath <- gsub(paste0("\\{", "knnName", "\\}"), URLencode(as.character(`knn.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        NearestNeighborsResults$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Knnnew = function(deployment.name, version.name, knn.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`knn.name`)) {
        stop("Missing required parameter `knn.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/knn/{knnName}/{versionName}/knnnew"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`knn.name`)) {
        urlPath <- gsub(paste0("\\{", "knnName", "\\}"), URLencode(as.character(`knn.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        NearestNeighborsResults$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ListAllExperiments = function(model.history.server.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiments"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExperimentEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Logfilepath = function(deployment.name, version.name, model.name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/logfilepath"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        character$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Login = function(login.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`login.request`)) {
        stop("Missing required parameter `login.request`.")
      }

      if (!missing(`login.request`)) {
        body <- `login.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/login"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        LoginResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Logs = function(deployment.name, version.name, model.name, log.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`log.request`)) {
        stop("Missing required parameter `log.request`.")
      }

      if (!missing(`log.request`)) {
        body <- `log.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/logs"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        LogBatch$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    MetaGet = function(deployment.name, version.name, model.name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/meta"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        object$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    MetaPost = function(content.type, deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`content.type`)) {
        stop("Missing required parameter `content.type`.")
      }

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      headerParams['Content-Type'] <- `content.type`

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/meta"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        object$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ModelStateChange = function(deployment.id, model.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      if (missing(`model.id`)) {
        stop("Missing required parameter `model.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment/{deploymentId}/model/{modelId}/state"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`model.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Models = function(deployment.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      urlPath <- "/deployment/{deploymentId}/models"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Modelset = function(deployment.name, version.name, model.name, file=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      body <- list(
        "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/modelset"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelStatus$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Modelupdate = function(deployment.name, version.name, model.name, file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      body <- list(
        "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/modelupdate"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelStatus$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Multiclassify = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/multiclassify"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        MultiClassClassificationResult$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Multipredict = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/multipredict"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        MultiPredictResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Multipredictimage = function(deployment.name, version.name, model.name, file, id, needs.preprocessing, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`needs.preprocessing`)) {
        stop("Missing required parameter `needs.preprocessing`.")
      }

      body <- list(
        "file" = httr::upload_file(file),
        "id" = id,
        "needs_preprocessing" = needs.preprocessing
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/multipredictimage"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        MultiPredictResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    NumRevisions = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      urlPath <- "/numrevisions"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        RevisionsWritten$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Predict = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predict"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Prediction$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    PredictError = function(content.type, operation, input.type, input.data=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`content.type`)) {
        stop("Missing required parameter `content.type`.")
      }

      if (missing(`operation`)) {
        stop("Missing required parameter `operation`.")
      }

      if (missing(`input.type`)) {
        stop("Missing required parameter `input.type`.")
      }

      headerParams['Content-Type'] <- `content.type`

      if (!missing(`input.data`)) {
        body <- `input.data`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/{operation}/{inputType}/error"
      if (!missing(`operation`)) {
        urlPath <- gsub(paste0("\\{", "operation", "\\}"), URLencode(as.character(`operation`), reserved = TRUE), urlPath)
      }

      if (!missing(`input.type`)) {
        urlPath <- gsub(paste0("\\{", "inputType", "\\}"), URLencode(as.character(`input.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    PredictV2File = function(operation, input.type.file, input.data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`operation`)) {
        stop("Missing required parameter `operation`.")
      }

      if (missing(`input.type.file`)) {
        stop("Missing required parameter `input.type.file`.")
      }

      if (missing(`input.data`)) {
        stop("Missing required parameter `input.data`.")
      }

      body <- list(
        "inputData" = httr::upload_file(input.data)
      )

      urlPath <- "/{operation}/{inputTypeFile}"
      if (!missing(`operation`)) {
        urlPath <- gsub(paste0("\\{", "operation", "\\}"), URLencode(as.character(`operation`), reserved = TRUE), urlPath)
      }

      if (!missing(`input.type.file`)) {
        urlPath <- gsub(paste0("\\{", "inputTypeFile", "\\}"), URLencode(as.character(`input.type.file`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    PredictV2Json = function(content.type, operation, input.type.json, input.data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`content.type`)) {
        stop("Missing required parameter `content.type`.")
      }

      if (missing(`operation`)) {
        stop("Missing required parameter `operation`.")
      }

      if (missing(`input.type.json`)) {
        stop("Missing required parameter `input.type.json`.")
      }

      if (missing(`input.data`)) {
        stop("Missing required parameter `input.data`.")
      }

      headerParams['Content-Type'] <- `content.type`

      if (!missing(`input.data`)) {
        body <- `input.data`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/{operation}/{inputTypeJson}"
      if (!missing(`operation`)) {
        urlPath <- gsub(paste0("\\{", "operation", "\\}"), URLencode(as.character(`operation`), reserved = TRUE), urlPath)
      }

      if (!missing(`input.type.json`)) {
        urlPath <- gsub(paste0("\\{", "inputTypeJson", "\\}"), URLencode(as.character(`input.type.json`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Predictimage = function(deployment.name, version.name, model.name, image=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      body <- list(
        "image" = httr::upload_file(image)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predictimage"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Prediction$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Predictwithpreprocess = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predictwithpreprocess"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Prediction$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Predictwithpreprocessjson = function(deployment.name, version.name, model.name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`model.name`)) {
        stop("Missing required parameter `model.name`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predictwithpreprocessjson"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), URLencode(as.character(`model.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JsonArrayResponse$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RawPredictBinary = function(input.type, output.type, input.data=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`input.type`)) {
        stop("Missing required parameter `input.type`.")
      }

      if (missing(`output.type`)) {
        stop("Missing required parameter `output.type`.")
      }

      body <- list(
        "inputData" = httr::upload_file(input.data)
      )

      urlPath <- "/raw/{inputType}/{outputType}"
      if (!missing(`input.type`)) {
        urlPath <- gsub(paste0("\\{", "inputType", "\\}"), URLencode(as.character(`input.type`), reserved = TRUE), urlPath)
      }

      if (!missing(`output.type`)) {
        urlPath <- gsub(paste0("\\{", "outputType", "\\}"), URLencode(as.character(`output.type`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RefreshJobStatus = function(job.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`job.id`)) {
        stop("Missing required parameter `job.id`.")
      }

      urlPath <- "/jobs/{jobId}/refresh"
      if (!missing(`job.id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), URLencode(as.character(`job.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JobEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RegisterUser = function(user, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`user`)) {
        stop("Missing required parameter `user`.")
      }

      if (!missing(`user`)) {
        body <- `user`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/user/register"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        User$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    ReimportModel = function(deployment.id, model.id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.id`)) {
        stop("Missing required parameter `deployment.id`.")
      }

      if (missing(`model.id`)) {
        stop("Missing required parameter `model.id`.")
      }

      if (missing(`body`)) {
        stop("Missing required parameter `body`.")
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment/{deploymentId}/model/{modelId}"
      if (!missing(`deployment.id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), URLencode(as.character(`deployment.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), URLencode(as.character(`model.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RevokeUserToken = function(token.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`token.id`)) {
        stop("Missing required parameter `token.id`.")
      }

      urlPath <- "/auth/token/{tokenId}"
      if (!missing(`token.id`)) {
        urlPath <- gsub(paste0("\\{", "tokenId", "\\}"), URLencode(as.character(`token.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Token$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Rollback = function(index, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`index`)) {
        stop("Missing required parameter `index`.")
      }

      urlPath <- "/rollback/{index}"
      if (!missing(`index`)) {
        urlPath <- gsub(paste0("\\{", "index", "\\}"), URLencode(as.character(`index`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        RollbackStatus$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    RunAJob = function(job.id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`job.id`)) {
        stop("Missing required parameter `job.id`.")
      }

      urlPath <- "/jobs/{jobId}/run"
      if (!missing(`job.id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), URLencode(as.character(`job.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        JobEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    TransformCsv = function(deployment.name, version.name, transform.name, batch.csv.record=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`transform.name`)) {
        stop("Missing required parameter `transform.name`.")
      }

      if (!missing(`batch.csv.record`)) {
        body <- `batch.csv.record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transform"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`transform.name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), URLencode(as.character(`transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        BatchCSVRecord$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Transformarray = function(deployment.name, version.name, transform.name, batch.record=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`transform.name`)) {
        stop("Missing required parameter `transform.name`.")
      }

      if (!missing(`batch.record`)) {
        body <- `batch.record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformarray"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`transform.name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), URLencode(as.character(`transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Base64NDArrayBody$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Transformimage = function(deployment.name, version.name, image.transform.name, files, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`image.transform.name`)) {
        stop("Missing required parameter `image.transform.name`.")
      }

      if (missing(`files`)) {
        stop("Missing required parameter `files`.")
      }

      body <- list(
        "files" = httr::upload_file(files)
      )

      urlPath <- "/endpoints/{deploymentName}/datavec/{imageTransformName}/{versionName}/transformimage"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`image.transform.name`)) {
        urlPath <- gsub(paste0("\\{", "imageTransformName", "\\}"), URLencode(as.character(`image.transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Base64NDArrayBody$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    TransformincrementalCsv = function(deployment.name, version.name, transform.name, single.csv.record=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`transform.name`)) {
        stop("Missing required parameter `transform.name`.")
      }

      if (!missing(`single.csv.record`)) {
        body <- `single.csv.record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformincremental"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`transform.name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), URLencode(as.character(`transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        SingleCSVRecord$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Transformincrementalarray = function(deployment.name, version.name, transform.name, single.record=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`transform.name`)) {
        stop("Missing required parameter `transform.name`.")
      }

      if (!missing(`single.record`)) {
        body <- `single.record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformincrementalarray"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`transform.name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), URLencode(as.character(`transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Base64NDArrayBody$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Transformincrementalimage = function(deployment.name, version.name, image.transform.name, file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`image.transform.name`)) {
        stop("Missing required parameter `image.transform.name`.")
      }

      if (missing(`file`)) {
        stop("Missing required parameter `file`.")
      }

      body <- list(
        "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/datavec/{imageTransformName}/{versionName}/transformincrementalimage"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`image.transform.name`)) {
        urlPath <- gsub(paste0("\\{", "imageTransformName", "\\}"), URLencode(as.character(`image.transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        Base64NDArrayBody$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    TransformprocessGet = function(deployment.name, version.name, transform.name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`transform.name`)) {
        stop("Missing required parameter `transform.name`.")
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformprocess"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`transform.name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), URLencode(as.character(`transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        object$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    TransformprocessPost = function(content.type, deployment.name, version.name, transform.name, transform.process=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`content.type`)) {
        stop("Missing required parameter `content.type`.")
      }

      if (missing(`deployment.name`)) {
        stop("Missing required parameter `deployment.name`.")
      }

      if (missing(`version.name`)) {
        stop("Missing required parameter `version.name`.")
      }

      if (missing(`transform.name`)) {
        stop("Missing required parameter `transform.name`.")
      }

      headerParams['Content-Type'] <- `content.type`

      if (!missing(`transform.process`)) {
        body <- `transform.process`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformprocess"
      if (!missing(`deployment.name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), URLencode(as.character(`deployment.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`version.name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), URLencode(as.character(`version.name`), reserved = TRUE), urlPath)
      }

      if (!missing(`transform.name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), URLencode(as.character(`transform.name`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        object$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateAuthPolicy = function(auth.policy, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`auth.policy`)) {
        stop("Missing required parameter `auth.policy`.")
      }

      if (!missing(`auth.policy`)) {
        body <- `auth.policy`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/auth/policy"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        AuthPolicy$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateBestModelForExperiment = function(model.history.server.id, update.best.model, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`update.best.model`)) {
        stop("Missing required parameter `update.best.model`.")
      }

      if (!missing(`update.best.model`)) {
        body <- `update.best.model`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/best"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExperimentEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateExperiment = function(model.history.server.id, experiment.id, experiment.entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`experiment.id`)) {
        stop("Missing required parameter `experiment.id`.")
      }

      if (missing(`experiment.entity`)) {
        stop("Missing required parameter `experiment.entity`.")
      }

      if (!missing(`experiment.entity`)) {
        body <- `experiment.entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`experiment.id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), URLencode(as.character(`experiment.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ExperimentEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateModelHistory = function(model.history.server.id, model.history.id, update.model.history.request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`model.history.server.id`)) {
        stop("Missing required parameter `model.history.server.id`.")
      }

      if (missing(`model.history.id`)) {
        stop("Missing required parameter `model.history.id`.")
      }

      if (missing(`update.model.history.request`)) {
        stop("Missing required parameter `update.model.history.request`.")
      }

      if (!missing(`update.model.history.request`)) {
        body <- `update.model.history.request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/modelhistory/{modelHistoryID}"
      if (!missing(`model.history.server.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), URLencode(as.character(`model.history.server.id`), reserved = TRUE), urlPath)
      }

      if (!missing(`model.history.id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), URLencode(as.character(`model.history.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        ModelHistoryEntity$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    UpdateUser = function(user, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`user`)) {
        stop("Missing required parameter `user`.")
      }

      if (!missing(`user`)) {
        body <- `user`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/user"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        User$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    },
    Upload = function(file=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      body <- list(
        "file" = httr::upload_file(file)
      )

      urlPath <- "/api/upload/model"
      # API key authentication
      if ("authorization" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["authorization"]) > 0) {
        headerParams['authorization'] <- paste(unlist(self$apiClient$apiKeys["authorization"]), collapse='')
      }
      # API key authentication
      if ("X-API-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-API-Key"]) > 0) {
        headerParams['X-API-Key'] <- paste(unlist(self$apiClient$apiKeys["X-API-Key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        FileUploadList$new()$fromJSONString(httr::content(resp, "text", encoding = "UTF-8"))
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }

    }
  )
)
