# Endpoints
#
# Endpoints API for different services in SKIL
#
# OpenAPI spec version: 1.2.0-rc1
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Default operations
#' @description skil_client.Default
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' accumulated_results Tells how many retraining examples have labels associated with them.
#'
#'
#' add_credentials Adds credentials
#'
#'
#' add_evaluation_result Adds an evaluation result
#'
#'
#' add_example_for_batch Adds a number of examples to a minibatch ID given an AddExampleRequest.
#'
#'
#' add_example_to_minibatch Adds an example to a minibatch
#'
#'
#' add_experiment Add an experiment, given an experiment entity
#'
#'
#' add_feedback_binary 
#'
#'
#' add_feedback_json Gets the retraining feedback for the given batch ID.
#'
#'
#' add_minibatch Adds a minibatch
#'
#'
#' add_model_feedback Adds an evaluation feedback to the model against a given minibatch id.
#'
#'
#' add_model_history Add a model history / workspace
#'
#'
#' add_model_instance Adds a model
#'
#'
#' add_resource Adds a resource
#'
#'
#' add_resource_group Adds a resource group
#'
#'
#' add_resource_to_group Adds a resource to a resource group
#'
#'
#' aggregate_model_results Aggregates the evaluaition results of a model instance, based on the evaluation type
#'
#'
#' change_user_password Change user&#39;s password
#'
#'
#' classify Use the deployed model to classify the input
#'
#'
#' classifyarray Same as /classify but returns the output as Base64NDArrayBody
#'
#'
#' classifyimage Use the deployed model to classify the input, using input image file from multipart form data.
#'
#'
#' clear_state Clears the accumulated data for retraining.
#'
#'
#' create_job Create a job
#'
#'
#' create_model_history Creates model History
#'
#'
#' delete_credentials_by_id Delete credentials given an ID
#'
#'
#' delete_experiment Deletes an experiment, given an experiment entity
#'
#'
#' delete_job_by_id Deletes a job given its ID
#'
#'
#' delete_model Delete a model by deployment and model id
#'
#'
#' delete_model_history Deletes a model history / workspace, given its ID
#'
#'
#' delete_model_instance Deletes a model instance, given its ID
#'
#'
#' delete_resource_by_id Delete the resource with the specified resource ID
#'
#'
#' delete_resource_from_group Removes a resource from a resource group
#'
#'
#' delete_resource_group_by_id Delete the resource group with the specified resource group ID
#'
#'
#' deploy_model Deploy a model in a deployment group.
#'
#'
#' deployment_create Create a new deployment group.
#'
#'
#' deployment_delete Delete a deployment by id
#'
#'
#' deployment_get Get a deployment details by id
#'
#'
#' deployments Get a list of deployments
#'
#'
#' detectobjects Detect the objects, given a (input) prediction request
#'
#'
#' download_job_output_file Download the output file from the job&#39;s execution. This will ONLY work if the job&#39;s run status is &#39;COMPLETE&#39;.
#'
#'
#' generate_auth_token Generate new auth token
#'
#'
#' get_all_jobs Get a list of all available jobs
#'
#'
#' get_array Get the memory mapped array based on the array type.
#'
#'
#' get_array_indices Get the memory mapped array indices based on the array type.
#'
#'
#' get_array_range Get the memory mapped array within a range based on the array type.
#'
#'
#' get_auth_policy Get auth policy
#'
#'
#' get_best_model_among_model_ids Gets the best model among the given model instance IDs, based on the evaluation type and column metric
#'
#'
#' get_credentials_by_id Get credentials given an ID
#'
#'
#' get_current_model Returns the current model being used for retraining.
#'
#'
#' get_evaluation_for_model_id Gets the list of evaluation results entity, given a model instance ID
#'
#'
#' get_examples_for_minibatch Gets all the examples for a minibatch ID
#'
#'
#' get_experiment Obtain an experiment&#39;s details, given its ID
#'
#'
#' get_experiments_for_model_history Obtain all experiments for a model history / workspace
#'
#'
#' get_job_by_id Get a job by its ID
#'
#'
#' get_last_evaluation Get the last evaluation specifications from the current model.
#'
#'
#' get_minibatch Gets a minibatch for the model
#'
#'
#' get_model_details Get model details
#'
#'
#' get_model_history Gets a model history, given its ID
#'
#'
#' get_model_instance Gets a model instance, given its ID
#'
#'
#' get_models_for_experiment Obtain a list of all the models for an experiment
#'
#'
#' get_resource_by_id Get the resource with the specified resource ID
#'
#'
#' get_resource_by_sub_type Get all the resources with the specified resource subtype
#'
#'
#' get_resource_by_type Get all the resources with the specified resource type
#'
#'
#' get_resource_details_by_id Get the resource details with the specified resource ID
#'
#'
#' get_resource_group_by_id Get the resource group with the specified resource group ID
#'
#'
#' get_resource_groups Get a list of all the resource groups
#'
#'
#' get_resources A list of all known/registered resources, of all types
#'
#'
#' get_resources_from_group Get all resources from a resource group
#'
#'
#' get_roles Get all roles.
#'
#'
#' get_user Get a user by user ID
#'
#'
#' get_user_auth_tokens Get auth tokens for a user.
#'
#'
#' get_users Get all users.
#'
#'
#' is_training Get the retraining status
#'
#'
#' jsonarray Run inference on the input and returns it as a JsonArrayResponse
#'
#'
#' knn Runs knn on the given index with the given k
#'
#'
#' knnnew Run a k nearest neighbors search on a NEW data point
#'
#'
#' list_all_experiments List all of the experiments in every model history / workspace
#'
#'
#' logfilepath Get logs file path
#'
#'
#' login Post JSON credentials and obtain a JWT authorization token.
#'
#'
#' logs Get logs
#'
#'
#' meta_get this method can be used to get the meta data for the current model which set to the server
#'
#'
#' meta_post This method can be used to set meta data for the current model which is set to the server
#'
#'
#' model_state_change Modify the state (start/stop) of a deployed model
#'
#'
#' models Retrieve a list of all the deployed models given a deployment id
#'
#'
#' modelset Set the model to be served
#'
#'
#' modelupdate Update the model to be served
#'
#'
#' multiclassify Represents all of the labels for a given classification
#'
#'
#' multipredict Get the output from the network, based on the given INDArray[] input
#'
#'
#' multipredictimage Get the output from the network using the given image file using the /multipredict endpoint&#39;s method
#'
#'
#' num_revisions Gets the number of retrained models written with retraining.
#'
#'
#' predict Run inference on the input array.
#'
#'
#' predict_error Runs inference and find invalid rows based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' predict_v2_file Runs inference based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' predict_v2_json Runs inference based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' predictimage Run inference on the input array, using input image file from multipart form data.
#'
#'
#' predictwithpreprocess Preprocesses the input and run inference on it
#'
#'
#' predictwithpreprocessjson Preprocesses the input and run inference on it and returns it as a JsonArrayResponse
#'
#'
#' raw_predict_binary Runs inference based on the input data. Output is defined relative to the output adapter specified.
#'
#'
#' refresh_job_status Refresh the remote job status. Can be used for monitoring.
#'
#'
#' register_user Register a new user
#'
#'
#' reimport_model Reimport a model to a previous deployed model in a deployment
#'
#'
#' revoke_user_token Revoke a user token.
#'
#'
#' rollback Rollback to a previous revision of the model.
#'
#'
#' run_a_job Start running an (already created) job on the remote resource
#'
#'
#' transform_csv Takes a BatchCSVRecord and returns the transformed array as BatchCSVRecord
#'
#'
#' transformarray Takes a batch input arrays and transforms it
#'
#'
#' transformimage Takes multiple multipart image file to transform and returns Base64NDArrayBody
#'
#'
#' transformincremental_csv Takes SingleCSVRecord as input and returns the transformed array as SingleCSVRecord
#'
#'
#' transformincrementalarray Same as /transformincremental but returns Base64NDArrayBody.
#'
#'
#' transformincrementalimage Takes a single multipart image file to transform and returns Base64NDArrayBody
#'
#'
#' transformprocess_get Gets the JSON string of the deployed transform process (CSV or Image)
#'
#'
#' transformprocess_post Sets the deployed (CSV or Image) transform process through the provided JSON string
#'
#'
#' update_auth_policy Update auth policy
#'
#'
#' update_best_model_for_experiment Updates the best model for an experiment
#'
#'
#' update_experiment Updates an experiment, given an experiment entity
#'
#'
#' update_model_history Update a model history / workspace
#'
#'
#' update_user Update a user
#'
#'
#' upload Upload a model file to SKIL for import.
#'
#' }
#'
#' @export
DefaultApi <- R6::R6Class(
  'DefaultApi',
  public = list(
    userAgent = "Swagger-Codegen/1.2.1.4/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    accumulated_results = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/accumulatedresults"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- AccumulatedResults$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_credentials = function(add_credentials_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`add_credentials_request`)) {
        body <- `add_credentials_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/resources/credentials"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ResourceCredentials$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_evaluation_result = function(model_history_server_id, evaluation_results_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`evaluation_results_entity`)) {
        body <- `evaluation_results_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/revisions/evaluations/"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- EvaluationResultsEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_example_for_batch = function(model_history_server_id, add_example_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`add_example_request`)) {
        body <- `add_example_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/exampleForBatch"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- AddExampleRequest$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_example_to_minibatch = function(model_history_server_id, example_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`example_entity`)) {
        body <- `example_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/example"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExampleEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_experiment = function(model_history_server_id, experiment_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`experiment_entity`)) {
        body <- `experiment_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExperimentEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_feedback_binary = function(id, type, file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "file" = httr::upload_file(file)
      )

      urlPath <- "/feedback/{id}/{type}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      if (!missing(`type`)) {
        urlPath <- gsub(paste0("\\{", "type", "\\}"), `type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FeedbackResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_feedback_json = function(id, labels, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`labels`)) {
        body <- `labels`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/feedback/{id}/json"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FeedbackResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_minibatch = function(model_history_server_id, minibatch_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`minibatch_entity`)) {
        body <- `minibatch_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/minibatch"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MinibatchEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_model_feedback = function(model_history_server_id, model_feed_back_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`model_feed_back_request`)) {
        body <- `model_feed_back_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/feedback"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelFeedBackRequest$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_model_history = function(model_history_server_id, add_model_history_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`add_model_history_request`)) {
        body <- `add_model_history_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/modelhistory"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelHistoryEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_model_instance = function(model_history_server_id, model_instance_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`model_instance_entity`)) {
        body <- `model_instance_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelInstanceEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_resource = function(add_resource_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`add_resource_request`)) {
        body <- `add_resource_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/resources/add/resource"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TODO_OBJECT_MAPPING$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_resource_group = function(group_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`group_name`)) {
        body <- `group_name`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/resources/add/group"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ResourceGroup$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    add_resource_to_group = function(resource_group_id, resource_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/add/resourcetogroup/{resourceGroupId}/{resourceId}"
      if (!missing(`resource_group_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), `resource_group_id`, urlPath)
      }

      if (!missing(`resource_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), `resource_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    aggregate_model_results = function(model_history_server_id, aggregate_prediction, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`aggregate_prediction`)) {
        body <- `aggregate_prediction`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/aggregateresults"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- EvaluationResultsEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    change_user_password = function(user_id, change_password_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`change_password_request`)) {
        body <- `change_password_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/user/{userId}/password"
      if (!missing(`user_id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- User$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classify = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/classify"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ClassificationResult$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classifyarray = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/classifyarray"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Base64NDArrayBody$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    classifyimage = function(deployment_name, version_name, model_name, image, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "image" = httr::upload_file(image)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/classifyimage"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ClassificationResult$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    clear_state = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/clear"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FeedbackResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_job = function(job_id_or_type, create_job_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`create_job_request`)) {
        body <- `create_job_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/jobs/{jobIdOrType}"
      if (!missing(`job_id_or_type`)) {
        urlPath <- gsub(paste0("\\{", "jobIdOrType", "\\}"), `job_id_or_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    create_model_history = function(model_history_server_id, model_history_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`model_history_entity`)) {
        body <- `model_history_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/revisions"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelHistoryEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_credentials_by_id = function(credential_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/credentials/{credentialId}"
      if (!missing(`credential_id`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), `credential_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_experiment = function(model_history_server_id, experiment_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`experiment_id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), `experiment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- InlineResponse200$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_job_by_id = function(job_id_or_type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/jobs/{jobIdOrType}"
      if (!missing(`job_id_or_type`)) {
        urlPath <- gsub(paste0("\\{", "jobIdOrType", "\\}"), `job_id_or_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_model = function(deployment_id, model_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/deployment/{deploymentId}/model/{modelId}"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      if (!missing(`model_id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), `model_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- InlineResponse200$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_model_history = function(model_history_server_id, model_history_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/modelhistory/{modelHistoryID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_history_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), `model_history_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- InlineResponse200$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_model_instance = function(model_history_server_id, model_instance_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/model/{modelInstanceID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_instance_id`)) {
        urlPath <- gsub(paste0("\\{", "modelInstanceID", "\\}"), `model_instance_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_resource_by_id = function(resource_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/remove/resource/{resourceId}"
      if (!missing(`resource_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), `resource_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Resource$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_resource_from_group = function(resource_group_id, resource_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/remove/resourcefromgroup/{resourceGroupId}/{resourceId}"
      if (!missing(`resource_group_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), `resource_group_id`, urlPath)
      }

      if (!missing(`resource_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), `resource_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_resource_group_by_id = function(resource_group_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/remove/group/{resourceGroupId}"
      if (!missing(`resource_group_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), `resource_group_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ResourceGroup$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    deploy_model = function(deployment_id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment/{deploymentId}/model"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    deployment_create = function(body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- DeploymentResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    deployment_delete = function(deployment_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/deployment/{deploymentId}"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- InlineResponse200$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    deployment_get = function(deployment_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/deployment/{deploymentId}"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- DeploymentResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    deployments = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/deployments"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- DeploymentResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    detectobjects = function(id, needs_preprocessing, threshold, file, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "id" = id,
          "needsPreprocessing" = needs_preprocessing,
          "threshold" = threshold,
          "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/detectobjects"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- DetectionResult$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    download_job_output_file = function(job_id, download_output_file_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`download_output_file_request`)) {
        body <- `download_output_file_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/jobs/{jobId}/outputfile"
      if (!missing(`job_id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), `job_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    generate_auth_token = function(token_generate_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`token_generate_request`)) {
        body <- `token_generate_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/auth/token"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Token$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_all_jobs = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/jobs"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_array = function(accept, array_type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`accept`)) {
        headerParams['accept'] <- `accept`
      }

      urlPath <- "/array/{arrayType}"
      if (!missing(`array_type`)) {
        urlPath <- gsub(paste0("\\{", "arrayType", "\\}"), `array_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_array_indices = function(content_type, accept, array_type, input, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`content_type`)) {
        headerParams['Content-Type'] <- `content_type`
      }

      if (!missing(`accept`)) {
        headerParams['accept'] <- `accept`
      }

      if (!missing(`input`)) {
        body <- `input`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/array/indices/{arrayType}"
      if (!missing(`array_type`)) {
        urlPath <- gsub(paste0("\\{", "arrayType", "\\}"), `array_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_array_range = function(accept, array_type, from, to, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`accept`)) {
        headerParams['accept'] <- `accept`
      }

      urlPath <- "/array/range/{from}/{to}/{arrayType}"
      if (!missing(`array_type`)) {
        urlPath <- gsub(paste0("\\{", "arrayType", "\\}"), `array_type`, urlPath)
      }

      if (!missing(`from`)) {
        urlPath <- gsub(paste0("\\{", "from", "\\}"), `from`, urlPath)
      }

      if (!missing(`to`)) {
        urlPath <- gsub(paste0("\\{", "to", "\\}"), `to`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_auth_policy = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/auth/policy"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- AuthPolicy$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_best_model_among_model_ids = function(model_history_server_id, best_model, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`best_model`)) {
        body <- `best_model`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/model/best"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelInstanceEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_credentials_by_id = function(credential_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/credentials/{credentialId}"
      if (!missing(`credential_id`)) {
        urlPath <- gsub(paste0("\\{", "credentialId", "\\}"), `credential_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ResourceCredentials$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_current_model = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/model"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_evaluation_for_model_id = function(model_history_server_id, model_instance_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/model/revisions/evaluations/{modelInstanceID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_instance_id`)) {
        urlPath <- gsub(paste0("\\{", "modelInstanceID", "\\}"), `model_instance_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- EvaluationResultsEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_examples_for_minibatch = function(model_history_server_id, minibatch_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/model/example/{minibatchId}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`minibatch_id`)) {
        urlPath <- gsub(paste0("\\{", "minibatchId", "\\}"), `minibatch_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExampleEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_experiment = function(model_history_server_id, experiment_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`experiment_id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), `experiment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExperimentEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_experiments_for_model_history = function(model_history_server_id, model_history_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/experiments/{modelHistoryID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_history_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), `model_history_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExperimentEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_job_by_id = function(job_id_or_type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/jobs/{jobIdOrType}"
      if (!missing(`job_id_or_type`)) {
        urlPath <- gsub(paste0("\\{", "jobIdOrType", "\\}"), `job_id_or_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_last_evaluation = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/lastevaluation"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- EvaluationResultsEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_minibatch = function(model_history_server_id, minibatch_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/model/minibatch/{minibatchId}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`minibatch_id`)) {
        urlPath <- gsub(paste0("\\{", "minibatchId", "\\}"), `minibatch_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MinibatchEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_model_details = function(deployment_id, model_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/deployment/{deploymentId}/model/{modelId}"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      if (!missing(`model_id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), `model_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_model_history = function(model_history_server_id, model_history_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/model/revision/{modelHistoryID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_history_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), `model_history_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelHistoryEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_model_instance = function(model_history_server_id, model_instance_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/model/{modelInstanceID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_instance_id`)) {
        urlPath <- gsub(paste0("\\{", "modelInstanceID", "\\}"), `model_instance_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelInstanceEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_models_for_experiment = function(model_history_server_id, experiment_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}/models"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`experiment_id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), `experiment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelInstanceEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_by_id = function(resource_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/resource/{resourceId}"
      if (!missing(`resource_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), `resource_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Resource$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_by_sub_type = function(resource_sub_type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/resources/subtype/{resourceSubType}"
      if (!missing(`resource_sub_type`)) {
        urlPath <- gsub(paste0("\\{", "resourceSubType", "\\}"), `resource_sub_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Resource$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_by_type = function(resource_type, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/resources/type/{resourceType}"
      if (!missing(`resource_type`)) {
        urlPath <- gsub(paste0("\\{", "resourceType", "\\}"), `resource_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Resource$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_details_by_id = function(resource_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/details/{resourceId}"
      if (!missing(`resource_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceId", "\\}"), `resource_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TODO_OBJECT_MAPPING$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_group_by_id = function(resource_group_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/group/{resourceGroupId}"
      if (!missing(`resource_group_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), `resource_group_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ResourceGroup$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_groups = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/groups"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ResourceGroup$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resources = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/resources"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Resource$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resources_from_group = function(resource_group_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/resources/group/{resourceGroupId}/resources"
      if (!missing(`resource_group_id`)) {
        urlPath <- gsub(paste0("\\{", "resourceGroupId", "\\}"), `resource_group_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Resource$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_roles = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/security/roles"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Role$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_user = function(user_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/user/{userId}"
      if (!missing(`user_id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- User$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_user_auth_tokens = function(user_id, only_valid, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`only_valid`)) {
        queryParams['onlyValid'] <- only_valid
      }

      urlPath <- "/auth/{userId}/tokens"
      if (!missing(`user_id`)) {
        urlPath <- gsub(paste0("\\{", "userId", "\\}"), `user_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Token$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_users = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/security/users"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- User$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    is_training = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/istraining"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RetrainingStatus$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    jsonarray = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/jsonarray"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JsonArrayResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    knn = function(deployment_name, version_name, knn_name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/knn/{knnName}/{versionName}/knn"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`knn_name`)) {
        urlPath <- gsub(paste0("\\{", "knnName", "\\}"), `knn_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- NearestNeighborsResults$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    knnnew = function(deployment_name, version_name, knn_name, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/knn/{knnName}/{versionName}/knnnew"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`knn_name`)) {
        urlPath <- gsub(paste0("\\{", "knnName", "\\}"), `knn_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- NearestNeighborsResults$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    list_all_experiments = function(model_history_server_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rpc/{modelHistoryServerId}/experiments"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExperimentEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    logfilepath = function(deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/logfilepath"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Character$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    login = function(login_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`login_request`)) {
        body <- `login_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/login"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- LoginResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    logs = function(deployment_name, version_name, model_name, log_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`log_request`)) {
        body <- `log_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/logs"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- LogBatch$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    meta_get = function(deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/meta"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MetaData$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    meta_post = function(content_type, body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`content_type`)) {
        headerParams['Content-Type'] <- `content_type`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/meta"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MetaData$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    model_state_change = function(deployment_id, model_id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment/{deploymentId}/model/{modelId}/state"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      if (!missing(`model_id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), `model_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    models = function(deployment_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/deployment/{deploymentId}/models"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    modelset = function(deployment_name, version_name, model_name, file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/modelset"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelStatus$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    modelupdate = function(file, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/modelupdate"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelStatus$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    multiclassify = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/multiclassify"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MultiClassClassificationResult$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    multipredict = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/multipredict"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MultiPredictResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    multipredictimage = function(file, id, needs_preprocessing, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "file" = httr::upload_file(file),
          "id" = id,
          "needs_preprocessing" = needs_preprocessing
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/multipredictimage"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- MultiPredictResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    num_revisions = function(...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/numrevisions"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RevisionsWritten$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predict = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predict"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Prediction$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predict_error = function(content_type, operation, input_type, input_data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`content_type`)) {
        headerParams['Content-Type'] <- `content_type`
      }

      if (!missing(`input_data`)) {
        body <- `input_data`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/{operation}/{inputType}/error"
      if (!missing(`operation`)) {
        urlPath <- gsub(paste0("\\{", "operation", "\\}"), `operation`, urlPath)
      }

      if (!missing(`input_type`)) {
        urlPath <- gsub(paste0("\\{", "inputType", "\\}"), `input_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predict_v2_file = function(operation, input_type_file, input_data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "inputData" = httr::upload_file(input_data)
      )

      urlPath <- "/{operation}/{inputTypeFile}"
      if (!missing(`operation`)) {
        urlPath <- gsub(paste0("\\{", "operation", "\\}"), `operation`, urlPath)
      }

      if (!missing(`input_type_file`)) {
        urlPath <- gsub(paste0("\\{", "inputTypeFile", "\\}"), `input_type_file`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predict_v2_json = function(content_type, operation, input_type_json, input_data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`content_type`)) {
        headerParams['Content-Type'] <- `content_type`
      }

      if (!missing(`input_data`)) {
        body <- `input_data`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/{operation}/{inputTypeJson}"
      if (!missing(`operation`)) {
        urlPath <- gsub(paste0("\\{", "operation", "\\}"), `operation`, urlPath)
      }

      if (!missing(`input_type_json`)) {
        urlPath <- gsub(paste0("\\{", "inputTypeJson", "\\}"), `input_type_json`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predictimage = function(deployment_name, version_name, model_name, image, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "image" = httr::upload_file(image)
      )

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predictimage"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Prediction$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predictwithpreprocess = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predictwithpreprocess"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Prediction$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    predictwithpreprocessjson = function(body, deployment_name, version_name, model_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/model/{modelName}/{versionName}/predictwithpreprocessjson"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`model_name`)) {
        urlPath <- gsub(paste0("\\{", "modelName", "\\}"), `model_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JsonArrayResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    raw_predict_binary = function(input_type, output_type, input_data, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "inputData" = httr::upload_file(input_data)
      )

      urlPath <- "/raw/{inputType}/{outputType}"
      if (!missing(`input_type`)) {
        urlPath <- gsub(paste0("\\{", "inputType", "\\}"), `input_type`, urlPath)
      }

      if (!missing(`output_type`)) {
        urlPath <- gsub(paste0("\\{", "outputType", "\\}"), `output_type`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    refresh_job_status = function(job_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/jobs/{jobId}/refresh"
      if (!missing(`job_id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), `job_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    register_user = function(user, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`user`)) {
        body <- `user`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/user/register"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- User$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    reimport_model = function(deployment_id, model_id, body, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/deployment/{deploymentId}/model/{modelId}"
      if (!missing(`deployment_id`)) {
        urlPath <- gsub(paste0("\\{", "deploymentId", "\\}"), `deployment_id`, urlPath)
      }

      if (!missing(`model_id`)) {
        urlPath <- gsub(paste0("\\{", "modelId", "\\}"), `model_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    revoke_user_token = function(token_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/auth/token/{tokenId}"
      if (!missing(`token_id`)) {
        urlPath <- gsub(paste0("\\{", "tokenId", "\\}"), `token_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Token$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    rollback = function(index, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/rollback/{index}"
      if (!missing(`index`)) {
        urlPath <- gsub(paste0("\\{", "index", "\\}"), `index`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- RollbackStatus$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    run_a_job = function(job_id, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/jobs/{jobId}/run"
      if (!missing(`job_id`)) {
        urlPath <- gsub(paste0("\\{", "jobId", "\\}"), `job_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- JobEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transform_csv = function(deployment_name, version_name, transform_name, batch_csv_record, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_csv_record`)) {
        body <- `batch_csv_record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transform"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`transform_name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), `transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- BatchCSVRecord$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformarray = function(deployment_name, version_name, transform_name, batch_record, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`batch_record`)) {
        body <- `batch_record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformarray"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`transform_name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), `transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Base64NDArrayBody$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformimage = function(deployment_name, version_name, image_transform_name, files, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "files" = files
      )

      urlPath <- "/endpoints/{deploymentName}/datavec/{imageTransformName}/{versionName}/transformimage"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`image_transform_name`)) {
        urlPath <- gsub(paste0("\\{", "imageTransformName", "\\}"), `image_transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Base64NDArrayBody$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformincremental_csv = function(deployment_name, version_name, transform_name, single_csv_record, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`single_csv_record`)) {
        body <- `single_csv_record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformincremental"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`transform_name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), `transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- SingleCSVRecord$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformincrementalarray = function(deployment_name, version_name, transform_name, single_record, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`single_record`)) {
        body <- `single_record`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformincrementalarray"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`transform_name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), `transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Base64NDArrayBody$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformincrementalimage = function(deployment_name, version_name, image_transform_name, file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "file" = httr::upload_file(file)
      )

      urlPath <- "/endpoints/{deploymentName}/datavec/{imageTransformName}/{versionName}/transformincrementalimage"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`image_transform_name`)) {
        urlPath <- gsub(paste0("\\{", "imageTransformName", "\\}"), `image_transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- Base64NDArrayBody$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformprocess_get = function(deployment_name, version_name, transform_name, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformprocess"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`transform_name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), `transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TODO_OBJECT_MAPPING$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    transformprocess_post = function(content_type, deployment_name, version_name, transform_name, transform_process, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`content_type`)) {
        headerParams['Content-Type'] <- `content_type`
      }

      if (!missing(`transform_process`)) {
        body <- `transform_process`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/endpoints/{deploymentName}/datavec/{transformName}/{versionName}/transformprocess"
      if (!missing(`deployment_name`)) {
        urlPath <- gsub(paste0("\\{", "deploymentName", "\\}"), `deployment_name`, urlPath)
      }

      if (!missing(`version_name`)) {
        urlPath <- gsub(paste0("\\{", "versionName", "\\}"), `version_name`, urlPath)
      }

      if (!missing(`transform_name`)) {
        urlPath <- gsub(paste0("\\{", "transformName", "\\}"), `transform_name`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- TODO_OBJECT_MAPPING$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_auth_policy = function(auth_policy, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`auth_policy`)) {
        body <- `auth_policy`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/auth/policy"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- AuthPolicy$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_best_model_for_experiment = function(model_history_server_id, update_best_model, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`update_best_model`)) {
        body <- `update_best_model`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/best"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExperimentEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_experiment = function(model_history_server_id, experiment_id, experiment_entity, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`experiment_entity`)) {
        body <- `experiment_entity`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/experiment/{experimentID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`experiment_id`)) {
        urlPath <- gsub(paste0("\\{", "experimentID", "\\}"), `experiment_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ExperimentEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_model_history = function(model_history_server_id, model_history_id, update_model_history_request, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`update_model_history_request`)) {
        body <- `update_model_history_request`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/rpc/{modelHistoryServerId}/modelhistory/{modelHistoryID}"
      if (!missing(`model_history_server_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryServerId", "\\}"), `model_history_server_id`, urlPath)
      }

      if (!missing(`model_history_id`)) {
        urlPath <- gsub(paste0("\\{", "modelHistoryID", "\\}"), `model_history_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- ModelHistoryEntity$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_user = function(user, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`user`)) {
        body <- `user`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/user"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- User$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    upload = function(file, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      body <- list(
          "file" = httr::upload_file(file)
      )

      urlPath <- "/api/upload/model"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FileUploadList$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
